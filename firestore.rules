rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // COMPLETELY OPEN RULES FOR DEBUGGING - FORCE UPDATE
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

